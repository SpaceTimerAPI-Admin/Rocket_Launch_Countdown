export function handleAvailabilityCheck(restaurant, date, time, user) {
    console.log(`⏳ Tracking: ${restaurant} on ${date} around ${time} for ${user.username}`);
    // Placeholder for Playwright scraping logic
}